@model IEnumerable<DigitaLibrary.Models.AcademicWork>
@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="container py-4">

   @*  <h2 class="mb-3">Kategoriler</h2>
    <div class="mb-4">
        @foreach (var cat in (IEnumerable<DigitaLibrary.Models.Category>)ViewBag.Categories)
        {
            <a asp-action="Index" asp-route-categoryId="@cat.Id"
               class="btn btn-sm btn-outline-primary me-2 mb-2">
                @cat.Name
            </a>
        }
    </div> *@

   @*  <form asp-action="Index" method="get" class="mb-4">
        <div class="input-group">
            <input type="text" name="q" value="@Context.Request.Query["q"]"
                   placeholder="Başlık, yazar, anahtar kelime..." class="form-control" />
            <button type="submit" class="btn btn-primary">Ara</button>
        </div>
    </form> *@

    <h2 class="mb-3">Son Çalışmalar</h2>
@if (!Model.Any())
{
        <div class="alert alert-info">Hiç çalışma bulunamadı.</div>
}
else
{
        <div class="row row-cols-1 row-cols-md-3 g-4">
            @foreach (var w in Model)
            {
                <div class="col">
                    <div class="card h-100 shadow-sm">
                        @if (!string.IsNullOrWhiteSpace(w.CoverImagePath))
                        {
                            <img src="@Url.Content(w.CoverImagePath)" class="card-img-top" alt="@w.Title" />
                        }
                        else
                        {
                            <img src="~/images/no-cover.png" class="card-img-top" alt="Kapak yok" />
                        }

                        <div class="card-body">
                            <h5 class="card-title">@w.Title</h5>
                            <p class="card-text text-muted" style="max-height: 70px; overflow:hidden;">
                                @w.Abstract
                            </p>
                        </div>

                        <div class="card-footer d-flex justify-content-between small text-muted">
                            <span>@w.Category?.Name</span>
                            <span>@w.CreatedAt.ToLocalTime().ToString("dd.MM.yyyy")</span>
                        </div>

                        <div class="card-footer">
                            <a asp-controller="AcademicWorks" asp-action="Details" asp-route-slug="@w.Slug"
                               class="btn btn-sm btn-outline-primary">Detay</a>

                            @if (User.Identity?.IsAuthenticated == true &&
                                                User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value == w.AuthorId)
                            {
                                <!-- Çalışmanın sahibi -->
                                <a asp-controller="AcademicWorks" asp-action="Edit" asp-route-id="@w.Id"
                                   class="btn btn-sm btn-outline-warning">Düzenle</a>
                            }
                            else
                            {
                                <!-- Başka kullanıcı görüyorsa -->
                                <a asp-controller="Profile" asp-action="ViewProfile" asp-route-id="@w.AuthorId"
                                   class="btn btn-sm btn-outline-secondary">Yazar</a>
                            }
                        </div>

                    </div>
                </div>
            }
        </div>

}
